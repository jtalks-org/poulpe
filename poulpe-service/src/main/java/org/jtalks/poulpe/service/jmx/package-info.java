/**
 * Contains beans that are going to be exposed via JMX so that it's possible to configure them in Runtime instead of
 * configuring them in usual files and restarting the whole app.
 */
package org.jtalks.poulpe.service.jmx;